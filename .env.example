# Copy to .env and fill in. .env is gitignored — never commit real keys.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: real retailer prices via SerpAPI Google Shopping (serpapi.com, free tier).
# Leave empty to fall back to Claude's price estimate.
SERPAPI_KEY=
